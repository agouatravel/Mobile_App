diff --git a/node_modules/expo-router/build/fork/useLinking.native.js b/node_modules/expo-router/build/fork/useLinking.native.js
index c080a58..e6ddd2c 100644
--- a/node_modules/expo-router/build/fork/useLinking.native.js
+++ b/node_modules/expo-router/build/fork/useLinking.native.js
@@ -124,7 +124,18 @@ function useLinking(ref, { enabled = true, prefixes, filter, config, getInitialU
                         const state = getStateFromURL(url);
                         if (typeof url === 'string') {
                             // If the link were handled, it gets cleared in NavigationContainer
-                            onUnhandledLinking((0, extractPathFromURL_1.extractExpoPathFromURL)(prefixes, url));
+                            // Patched: defer past the current mount/commit cycle so this doesn't
+                            // fire before NavigationContainer has finished mounting (avoids
+                            // "Can't perform a React state update on a component that hasn't
+                            // mounted yet"). A single setTimeout(0) wasn't reliably enough with a
+                            // heavier provider tree, so drain the immediate queue first, then add a
+                            // fallback delay. (This is exactly what the deprecated
+                            // InteractionManager.runAfterInteractions stub does in RN 0.86.)
+                            setImmediate(() => {
+                                setTimeout(() => {
+                                    onUnhandledLinking((0, extractPathFromURL_1.extractExpoPathFromURL)(prefixes, url));
+                                }, 100);
+                            });
                         }
                         return state;
                     });
