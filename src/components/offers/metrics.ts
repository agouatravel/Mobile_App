// Shared layout numbers for the Offers screen and the cards it composes.
// They live here rather than in offers.tsx so the card components can size
// their own corners and gutters without importing the screen.
export const SIDE_PADDING = 20;
export const GAP = 12;
export const CARD_RADIUS = 22;
